export default function LegalPrivacySectionHeadLine({ children }) {
  return (
    <h2 className="text-primary text-lg font-[Inter] font-bold tracking-wide sm:text-xl">
      {children}
    </h2>
  );
}
